apiVersion: v1
kind: Service
metadata:
  name: k8s-ui-backend
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: k8s-ui-backend
  ports:
    - name: http
      port: 3001
      targetPort: 3001
  type: ClusterIP
